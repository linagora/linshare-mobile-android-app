image: jangrewe/gitlab-ci-android

before_script:
  - export GRADLE_USER_HOME=$(pwd)/.gradle
  - chmod +x ./gradlew

cache:
  key: "$CI_PIPELINE_ID"
  paths:
    - .gradle/

stages:
  - linter
  - build
  - test
  - publish

lintDebug:
  stage: linter
  tags:
    - docker
  script:
    - ./gradlew -Pci --console=plain :presentation:lintDebug -PbuildDir=lint

ktlint:
  stage: linter
  tags:
    - docker
  script:
    - ./gradlew -Pci --console=plain ktlint

assembleDebug:
  stage: build
  tags:
    - docker
  script:
    - ./gradlew assembleDebug

testDebug:
  stage: test
  tags:
    - docker
  script:
    - ./gradlew -Pci --console=plain clean :domain:testDebug :presentation:testDebug :data:testDebug

publishAPK:
  stage: publish
  tags:
    - docker
  script:
    - ./gradlew -Pci --console=plain clean assemble
  environment:
    name: master
  artifacts:
    name: "linshare-apk-for-test"
    expire_in: 1 month
    paths:
      - presentation/build/outputs/apk/develop/LinShare*.apk
  only:
    - master