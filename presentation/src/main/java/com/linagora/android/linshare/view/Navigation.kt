package com.linagora.android.linshare.view

class Navigation {

    enum class LoginFlow {
        DIRECT,
        INDIRECT
    }

    enum class UploadType {
        OUTSIDE_APP,
        INSIDE_APP
    }
}
